// Importiere hier die benötigten Funktionen



// Mögliche Vorgehensweise: Beispiel für die erste Rechenperation (Modulo)
function moduloInit() {

  const moduloForm = (document.querySelector('#modulo-form');
  const moduloResult = document.querySelector('#modulo_result');

  moduloForm.addEventListener('input', ()=>{
     // setze hier das Ergebnis der Operation in das moduloResult input Feld
     // verwende zu Berechnung die modulo Funktion aus der percentage.js Datei

  });
}

moduloInit();